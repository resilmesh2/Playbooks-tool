openapi: 3.0.0
info:
  title: OpenC2 Command Orchestrator
  description: API for creating an OpenC2 command as a Producer to deliver it on
    an OpenC2 Consumer
  version: "1.0"
  x-logo: ""
  contact:
    name: "@Anonymous Shuffle User"
    url: https://twitter.com/shuffleio
    email: support@shuffler.io
servers:
  - url: ""
basePath: /
schemes:
  - ""
paths:
  /close_connection:
    post:
      responses:
        default:
          description: default
          content:
            text/plain:
              schema:
                type: string
                example: ""
      summary: Close a TCP connection on OpenC2 consumeractuator
      operationId: Close_a_TCP_connection_on_OpenC2_consumeractuator
      description: Send an OpenC2 command to consumer to close a TCP connection
      parameters:
        - in: body
          name: body
          multiline: true
          description: Generated by shuffler.io OpenAPI
          required: false
          example: |-
            {
              "actuator_ip": "${actuator_ip}",
              "dst_ip": "${dst_ip}",
              "dst_port": "${dst_port}",
              "pid": "${pid}",
              "src_port": "${src_port}"
            }
          schema:
            type: string
      requestBody:
        description: Generated by Shuffler.io
        required: false
        content:
          example:
            example: |-
              {
                "actuator_ip": "${actuator_ip}",
                "dst_ip": "${dst_ip}",
                "dst_port": "${dst_port}",
                "pid": "${pid}",
                "src_port": "${src_port}"
              }
  /delete_file:
    post:
      responses:
        default:
          description: default
          content:
            text/plain:
              schema:
                type: string
                example: ""
      summary: Delete a file on OpenC2 consumeractuator
      operationId: Delete_a_file_on_OpenC2_consumeractuator
      description: Send an OpenC2 command to consumer to delete a file
      parameters:
        - in: body
          name: body
          multiline: true
          description: Generated by shuffler.io OpenAPI
          required: false
          example: |-
            {
              "actuator_ip": "${actuator_ip}",
              "file_path": "${file_path}"
            }
          schema:
            type: string
      requestBody:
        description: Generated by Shuffler.io
        required: false
        content:
          example:
            example: |-
              {
                "actuator_ip": "${actuator_ip}",
                "file_path": "${file_path}"
              }
components:
  securitySchemes: {}
